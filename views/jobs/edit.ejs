<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("../partials/head") %>
  <title>Edit Job — SkillForge</title>
</head>
<body>
  <div class="app-wrapper">
    <%- include("../partials/app-sidebar", { activePage: 'jobs' }) %>

    <main class="app-main">
      <%- include("../partials/app-topbar", { pageTitle: 'Edit Job Opportunity' }) %>

      <div class="app-content animate-fade-in" style="max-width: 700px;">
        <div class="page-header">
          <div>
            <h1 class="page-title">Edit Job Application</h1>
            <p class="page-subtitle">Update details for <%= job.company %> (<%= job.role %>).</p>
          </div>
          <div class="page-actions">
            <a href="/jobs" class="btn btn-secondary">
              <i data-lucide="arrow-left"></i>
              <span>Back to Kanban</span>
            </a>
          </div>
        </div>

        <div class="card">
          <form action="/jobs/edit/<%= job._id %>" method="POST">
            <div class="form-grid">
              <div class="form-group">
                <label class="form-label" for="company">Company Name *</label>
                <input type="text" id="company" name="company" class="form-control" value="<%= job.company %>" required>
              </div>

              <div class="form-group">
                <label class="form-label" for="role">Job Role / Title</label>
                <input type="text" id="role" name="role" class="form-control" value="<%= job.role %>">
              </div>
            </div>

            <div class="form-grid">
              <div class="form-group">
                <label class="form-label" for="status">Pipeline Stage</label>
                <select id="status" name="status" class="form-select">
                  <% STAGES.forEach(s => { %>
                  <option value="<%= s %>" <%= job.status === s ? "selected" : "" %>><%= s %></option>
                  <% }) %>
                </select>
              </div>

              <div class="form-group">
                <label class="form-label" for="location">Location</label>
                <input type="text" id="location" name="location" class="form-control" value="<%= job.location %>">
              </div>
            </div>

            <div class="form-grid">
              <div class="form-group">
                <label class="form-label" for="salary">Compensation / Salary</label>
                <input type="text" id="salary" name="salary" class="form-control" value="<%= job.salary %>">
              </div>

              <div class="form-group">
                <label class="form-label" for="applyLink">Application / Job Post URL</label>
                <input type="url" id="applyLink" name="applyLink" class="form-control" value="<%= job.applyLink %>">
              </div>
            </div>

            <div class="form-group">
              <label class="form-label" for="notes">Notes & Interview Details</label>
              <textarea id="notes" name="notes" class="form-textarea"><%= job.notes %></textarea>
            </div>

            <div style="display: flex; gap: 0.75rem; justify-content: flex-end; margin-top: 1.5rem; border-top: 1px solid var(--border-subtle); padding-top: 1.25rem;">
              <a href="/jobs" class="btn btn-secondary">Cancel</a>
              <button type="submit" class="btn btn-primary">
                <i data-lucide="check"></i>
                <span>Update Job</span>
              </button>
            </div>
          </form>
        </div>
      </div>
    </main>
  </div>

  <%- include("../partials/scripts") %>
</body>
</html>
